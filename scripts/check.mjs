import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules'].includes(e.name))continue;const p=path.join(d,e.name);e.isDirectory()?walk(p):e.name.endsWith('.html')&&files.push(p)}}walk(root);const errors=[];for(const f of files){const s=fs.readFileSync(f,'utf8');if(!/assets\/styles\.css/.test(s))errors.push(`${f}: CSS absent`);if(!/noindex,follow/.test(s))errors.push(`${f}: noindex absent`);for(const href of s.matchAll(/href="([^"#]+)"/g)){const u=href[1];if(/^(https?:|mailto:)/.test(u))continue;const target=path.resolve(path.dirname(f),u);if(!fs.existsSync(target)&&!fs.existsSync(path.join(target,'index.html')))errors.push(`${f}: lien cassé ${u}`)}}if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`${files.length} pages contrôlées : CSS, noindex et liens internes valides.`)
